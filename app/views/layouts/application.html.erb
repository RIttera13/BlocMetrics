<!DOCTYPE html>
<html>
<head>
  <title>Blocmetrics</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-default">
    <% if user_signed_in? %>
      <div class="pull-right user-info">
        Signed in as: <strong><%= current_user.email %></strong> |
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link pull-right login-right'  %>
      </div>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link pull-right user-info' %>  -
      <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link pull-right login-right user-info' %>
    <% end %>
  </div>
  <div>
    <p class="alert-notice text-center"><%= notice %></p>
    <p class="alert-alert text-center"><%= alert %></p>
  </div>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
